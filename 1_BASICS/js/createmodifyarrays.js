// Create an Array

var array = [1,2,3];


// How many values inside the Array
console.log(array.length);

// Console log the third one
console.log(array[2]);

// Change the third one
array[2] = 7184


// Add a fourth value
array[3] = 4817


// Add a sixth value
array[5] = 2609;


// Add a value at the end of the array
array.push(42)

// Remove the last value of the array
// array.pop()

// Assign the last value into a variable
// var single = array.pop()
// console.log(single)

// Remove the first value of the array
// array.shift();

// Assign the first value into a variable
// var single = array.shift();
// console.log(single)

// Add a value at teh beginning of the array


console.log(array);

let abe = 10;
console.log(abe)